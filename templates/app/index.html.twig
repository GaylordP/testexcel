{% extends 'app/_template.html.twig' %}

{% block title %}
  Résultat d'un questionnaire
{% endblock %}

{% block content %}
  <div class="card">
    <div class="card-body">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type"submit" class="btn btn-primary">
          Envoyer
        </button>
        {{ form_end(form) }}
    </div>
  </div>
  </table>
{% endblock %}